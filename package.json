{
  "name": "soccer-score-tracker",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "deploy": "node build.js && gh-pages -d dist --dotfiles"
  },
  "devDependencies": {
    "gh-pages": "^6.2.0"
  }
}
